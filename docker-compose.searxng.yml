services:
  searxng:
    image: searxng/searxng:latest
    container_name: thebestappeva-searxng
    ports:
      - "127.0.0.1:8888:8080"
    restart: unless-stopped
    volumes:
      - ./searxng/settings.yml:/etc/searxng/settings.yml:ro
