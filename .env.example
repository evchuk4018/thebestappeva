# No API key is required for the local Ollama AI tab.
# Run Ollama locally and keep its HTTP server available at http://127.0.0.1:11434.
# Run SearXNG locally with `docker compose -f docker-compose.searxng.yml up -d`.

SEARXNG_BASE_URL=http://127.0.0.1:8888
PORT=3000
WEB_SEARCH_TIMEOUT_MS=10000
URL_FETCH_TIMEOUT_MS=12000
WEB_SEARCH_MAX_RESULTS=8
URL_FETCH_MAX_CHARS=12000
